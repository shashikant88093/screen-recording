function recordHandle(e){
    // Create  a MediaStream Recording API instance
   const mediaRecorder = new MediaRecorder(stream);

   // create a data array to store the recorded data
   const recordedData = [];

//     


}